import React from 'react'
import PropTypes from 'prop-types'

/**
 * Footer. 'Nuf said.
 * @param Object props Any props passed from the parent
 */
const Footer = props => {
  return <div>Footer</div>
}

Footer.propTypes = {}

export default Footer
